version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DOPPLER_TOKEN: ${DOPPLER_TOKEN}
    image: demo-cabinet-test:latest
    ports:
      - "3000:3000"
    environment:
      - DOPPLER_TOKEN=${DOPPLER_TOKEN}