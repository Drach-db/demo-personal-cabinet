server {
    listen 3000;
    root /usr/share/nginx/html;
    
    location / {
        try_files /pages/team/index.html =404;
    }
    
    location /pages/ {
        try_files $uri $uri/ =404;
    }
    
    location /assets/ {
        try_files $uri =404;
    }
}