version: '3.8'
services:
  app:
    # Ваш реальный username без скобок!
    image: ghcr.io/drach-db/demo-personal-cabinet:latest
    ports:
      - "3000:3000"
    environment:
      - DOPPLER_TOKEN=${DOPPLER_TOKEN}
    restart: unless-stopped